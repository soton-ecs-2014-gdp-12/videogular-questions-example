'use strict';

describe('videogularQuizExample.questionRange module', function() {

  beforeEach(module('videogularQuizExample.questionRange'));

  describe('QuestionRange controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var questionRangeCtrl = $controller('QuestionRangeCtrl');
      expect(questionRangeCtrl).toBeDefined();
    }));

  });
});
