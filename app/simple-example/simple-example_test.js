'use strict';

describe('videogularQuizExample.simpleExample module', function() {

  beforeEach(module('videogularQuizExample.simpleExample'));

  describe('SimpleExample controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var simpleExampleCtrl = $controller('SimpleExampleCtrl');
      expect(simpleExampleCtrl).toBeDefined();
    }));

  });
});
