'use strict';

describe('videogularQuizExample.questionMultiple module', function() {

  beforeEach(module('videogularQuizExample.questionMultiple'));

  describe('QuestionMultiple controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var questionMultipleCtrl = $controller('QuestionMultipleCtrl');
      expect(questionMultipleCtrl).toBeDefined();
    }));

  });
});
