'use strict';

describe('videogularQuizExample.questionStars module', function() {

  beforeEach(module('videogularQuizExample.questionStars'));

  describe('QuestionStars controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var questionStarsCtrl = $controller('QuestionStarsCtrl');
      expect(questionStarsCtrl).toBeDefined();
    }));

  });
});
