'use strict';

describe('videogularQuizExample.pollSimple module', function() {

  beforeEach(module('videogularQuizExample.pollSimple'));

  describe('PollSimple controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var pollSimpleCtrl = $controller('PollSimpleCtrl');
      expect(pollSimpleCtrl).toBeDefined();
    }));

  });
});
