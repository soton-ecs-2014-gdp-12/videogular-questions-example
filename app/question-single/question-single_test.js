'use strict';

describe('videogularQuizExample.questionSingle module', function() {

  beforeEach(module('videogularQuizExample.questionSingle'));

  describe('QuestionSingle controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var questionSimpleCtrl = $controller('QuestionSingleCtrl');
      expect(questionSimpleCtrl).toBeDefined();
    }));

  });
});
