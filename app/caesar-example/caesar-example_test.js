'use strict';

describe('videogularQuizExample.caesarExample module', function() {

  beforeEach(module('videogularQuizExample.caesarExample'));

  describe('SimpleExample controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var caesarExampleCtrl = $controller('SimpleExampleCtrl');
      expect(caesarExampleCtrl).toBeDefined();
    }));

  });
});
