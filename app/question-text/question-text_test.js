'use strict';

describe('videogularQuizExample.questionText module', function() {

  beforeEach(module('videogularQuizExample.questionText'));

  describe('QuestionText controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var questionTextCtrl = $controller('QuestionTextCtrl');
      expect(questionTextCtrl).toBeDefined();
    }));

  });
});
